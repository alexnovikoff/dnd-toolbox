// Font stacks used in inline styles across the design system and modules.
// Mirror --ds-font-sans / --ds-font-serif from tokens.css.
export const SANS = '"Public Sans", system-ui, sans-serif';
export const SERIF = '"Cinzel", Georgia, serif';
