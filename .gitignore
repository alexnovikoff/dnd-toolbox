# dependencies
node_modules/

# build output
dist/
.vercel/
coverage/

# env (never commit real keys)
.env
.env.local
.env.*.local

# secrets / ssh keys — never commit
claudekey
claudekey.pub
id_rsa*
id_ed25519*
*.pem
*.key

# logs / os
*.log
.DS_Store

# local Claude memory (not shared)
.claude.local.md
